#pragma once

enum class PacketType
{
	PLAYERSET,
	STATECHANGE,
	PLAYERUPDATE,
	GAMEUPDATE,
	GAMEEND
};

enum class GameState
{
	SELECT,
	SELECTIP,
	WAITING,
	START,
	GAMEPLAY,
	GAMEOVER,
	SERVERCLOSED
};